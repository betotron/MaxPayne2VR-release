# DXVK 3.0.2 (x32) - Max Payne 2 VR
#
# ES  Este archivo lo lee DXVK, no el mod. Vacio esta bien: la configuracion
#     de fabrica es la que se ha probado.
#
#     Para ver el HUD de DXVK dentro del casco -version, tarjeta y fotogramas
#     por segundo-, quita la almohadilla de la linea de abajo:
#
# EN  This file is read by DXVK, not by the mod. Empty is fine: the default
#     configuration is the one that has been tested.
#
#     To see the DXVK HUD inside the headset - version, GPU and frames per
#     second - remove the hash from the line below:
#
# dxvk.hud = version,devinfo,fps
